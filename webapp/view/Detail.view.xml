<mvc:View
    controllerName="be.ap.edu.zsd041peoplelist.controller.Detail"
    xmlns="sap.m"
    xmlns:semantic="sap.f.semantic"
    xmlns:mvc="sap.ui.core.mvc">

    <semantic:SemanticPage
        id="detailPage"
        busy="{detailView>/busy}"
        busyIndicatorDelay="{detailView>/delay}">
        <semantic:titleHeading>
                <Title id="_IDGenTitle1"
                text="{Firstname} {Name}"
                type="H2"/>
        </semantic:titleHeading>
        <semantic:headerContent>
        <ObjectAttribute id="_IDGenObjectAttribute1" text = "{Type}" />
        <ObjectAttribute id="_IDGenObjectAttribute2" 
            text="{
                path: 'DateOfBirth', 
                type: 'sap.ui.model.type.Date',
                formatOptions: { style: 'full',
                                 source: { pattern: 'yyyy/MM/dd'} 
                        }
                    }"/>
            <ObjectNumber id="_IDGenObjectNumber1" />
        </semantic:headerContent>
        <semantic:content>

        </semantic:content>
        <!--Semantic ShareMenu Buttons-->
        <semantic:sendEmailAction>
            <semantic:SendEmailAction
                id="shareEmail"
                press=".onSendEmailPress"/>
        </semantic:sendEmailAction>

        <semantic:closeAction>
            <semantic:CloseAction
                    id="closeColumn"
                    press=".onCloseDetailPress"/>
        </semantic:closeAction>

        <semantic:fullScreenAction>
            <semantic:FullScreenAction
                    id="enterFullScreen"
                    visible="{= !${device>/system/phone} &amp;&amp; !${appView>/actionButtonsInfo/midColumn/fullScreen}}"
                    press=".toggleFullScreen"/>
        </semantic:fullScreenAction>
        <semantic:exitFullScreenAction>
            <semantic:ExitFullScreenAction
                    id="exitFullScreen"
                    visible="{= !${device>/system/phone} &amp;&amp; ${appView>/actionButtonsInfo/midColumn/fullScreen}}"
                    press=".toggleFullScreen"/>
        </semantic:exitFullScreenAction>

    </semantic:SemanticPage>

</mvc:View>